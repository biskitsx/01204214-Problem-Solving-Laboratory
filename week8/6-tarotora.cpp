#include<iostream>
#include<vector>
using namespace std ;

int c, n, k , a, b ;
vector<int> powerA , powerB;


int main() {
    cin >> c ;

    for (int i = 0 ; i < c ; i++) {
        cin >> n >> k ;
        powerA.resize(n+1) ;
        powerB.resize(n+1) ;
        for (int j = 0 ; j < n ; j++) {
            cin >> a[j] >> b[j] ;
        }

    }
}